import { formatMoney } from "src/utils/format"

class Business {
    static Dropship = {
        fee: {
            number: 5900,
            string: formatMoney(5900)
        }
    }
}

export default Business